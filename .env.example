# =============================================================================
# DESARROLLO LOCAL (tu computadora)
# =============================================================================
# Opción 1: PostgreSQL instalado en tu PC
#   DATABASE_URL="postgresql://hotelcosta:TU_PASSWORD@localhost:5432/hotelcosta"
# Opción 2: Base en la nube (Neon, Supabase, etc.) — misma URL en .env local
#   DATABASE_URL="postgresql://user:pass@ep-xxx.region.provider.tech/dbname?sslmode=require"
#
# Requerido para el login (sesión JWT). Generar con: openssl rand -base64 32
# NEXTAUTH_URL="http://localhost:3000"
# NEXTAUTH_SECRET="pegá-acá-el-resultado-de-openssl-rand-base64-32"
# =============================================================================

# =============================================================================
# VPS (producción) — App y PostgreSQL en el MISMO servidor
# =============================================================================
# En el VPS creas un archivo .env (o .env.production) con estos valores.
# PostgreSQL se instala EN EL VPS y solo escucha en localhost (no expones 5432).
# La app conecta a "localhost" porque la base está en el mismo VPS.
#
# DATABASE_URL="postgresql://hotelcosta:PASSWORD_SEGURO_20_CHARS@localhost:5432/hotelcosta"
# NEXTAUTH_URL="https://sistema.hoteldelacosta.cl"
# NEXTAUTH_SECRET="generar-con-openssl-rand-base64-32"
#
# Integración MotoPress (reservas desde la web del hotel)
# MOTOPRESS_URL="https://hoteldelacosta.cl"
# MOTOPRESS_CONSUMER_KEY="ck_xxxx..."
# MOTOPRESS_CONSUMER_SECRET="cs_xxxx..."
# SYNC_SECRET="generar-con-openssl-rand-base64-32"
# (Opcional) Si tienes varios establecimientos: MOTOPRESS_ESTABLISHMENT_ID="cuid-del-establishment"
#
# NODE_ENV="production"
# =============================================================================

DATABASE_URL="postgresql://usuario:password@localhost:5432/hotelcosta"
